// Package network implements container networking (bridge and host modes).
// Bridge mode creates a virtual bridge with veth pairs and iptables NAT rules.
// Host mode reuses the host network namespace directly.
//
// Phase 3.2 — to be implemented.
package network
