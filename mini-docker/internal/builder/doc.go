// Package builder implements the Dockerfile builder.
// It parses Dockerfile instructions (FROM, RUN, COPY, CMD, etc.) and builds
// OCI-compatible images layer by layer using mini-containerd as the backend.
//
// Phase 3.1 — to be implemented.
package builder
